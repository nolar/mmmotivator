[project]
name = "mmmai-api"
version = "0.1.0"
description = "FastAPI front-door for the AI backend"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "openai",
    "pydantic",
    "redis",
]
